name: Deploy Frontend to Azure Static Web Apps

on:
  push:
    branches: [main]
    paths:
      - 'frontend/**'   # solo se dispara si cambias algo en /frontend

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout código
        uses: actions/checkout@v4

      - name: Desplegar en Azure Static Web Apps
        uses: Azure/static-web-apps-deploy@v1
        with:
          azure_static_web_apps_api_token: ${{ secrets.SWA_DEPLOYMENT_TOKEN }}
          repo_token: ${{ secrets.GITHUB_TOKEN }}
          action: 'upload'
          app_location: 'frontend'
          output_location: ''
```

---

## 7. El flujo de trabajo desde ahora

Una vez configurado todo, tu flujo diario en VS Code será simplemente:
```
Editas código → Ctrl+Shift+G (Source Control) → escribes mensaje de commit → Commit → Sync